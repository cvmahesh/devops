✅ Sample Full-Stack App (Node.js Backend + Simple Frontend)

This example shows:

A frontend (HTML + JS) that calls the backend

A backend using Node.js + Express

A simple API: /api/message that returns JSON

📌 1. Backend (Node.js + Express)

Create a folder → simple-app
Inside it, create two files:

A. package.json (optional)

Run this in your terminal:

```
npm init -y
npm install express
npm install cors
```



📌 2. Frontend (HTML + JavaScript)

Create a file → index.html


📌  3. how to run
```
node server.js
```


Now your backend is live →
Open your browser and go to:

http://localhost:3000/api/message